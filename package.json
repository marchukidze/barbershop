{
  "name": "barbershop-web",
  "version": "0.1.0",
  "description": "barbershop web",
  "main": "index.js",
  "scripts": {
    "gulp:sass": "gulp sass-to-css",
    "gulp:sass:watch": "gulp sass-watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "irma",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "gulp": "^4.0.2",
    "gulp-sass": "^4.1.0"
  }
}
